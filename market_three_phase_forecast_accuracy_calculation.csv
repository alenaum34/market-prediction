import pandas as pd

# завантаження файлу
df = pd.read_csv("forecast_of_market_dataset.csv")

# перетворюємо дату
df["month"] = pd.to_datetime(df["month"])

# сортуємо
df = df.sort_values(["product_group_id", "month"])

# створюємо прогноз (як у типовому прикладі — середнє за 3 місяці)
df["forecast"] = df.groupby("product_group_id")["market_revenue"]\
                   .rolling(3).mean().reset_index(0, drop=True)

# рахуємо помилку
df["error"] = df["market_revenue"] - df["forecast"]

# абсолютна помилка
df["abs_error"] = df["error"].abs()

# MAPE
df["mape"] = df["abs_error"] / df["market_revenue"] * 100

# фінальна таблиця (як зазвичай у прикладах)
final_table = df.groupby("product_group_name")[["market_revenue", "forecast", "mape"]]\
                .mean().reset_index()

# округлення для красивого вигляду
final_table = final_table.round(2)

# вивід
print(final_table)

# збереження
final_table.to_csv("final_forecast_table.csv", index=False)
