import pandas as pd

# 1. Завантаження даних
df = pd.read_csv("forecast_of_market_dataset.csv")

# 2. Перетворення дати
df["month"] = pd.to_datetime(df["month"])

# 3. Сортування
df = df.sort_values(by=["product_group_id", "month"])

# 4. Розрахунок частки Amazon
df["amazon_share"] = df["revenue_amazon"] / df["market_revenue"]

# 5. Додаємо рік і місяць
df["year"] = df["month"].dt.year
df["month_num"] = df["month"].dt.month

# 6. Простий forecast (наприклад: середнє за попередні 3 місяці)
df["forecast_market"] = (
    df.groupby("product_group_id")["market_revenue"]
    .rolling(window=3)
    .mean()
    .reset_index(0, drop=True)
)

# 7. Помилка прогнозу
df["forecast_error"] = df["market_revenue"] - df["forecast_market"]

# 8. Абсолютна помилка
df["abs_error"] = df["forecast_error"].abs()

# 9. MAPE (у %)
df["mape"] = (df["abs_error"] / df["market_revenue"]) * 100

# 10. Результат по категоріях
result = df.groupby("product_group_name")["mape"].mean().reset_index()

# 11. Збереження
result.to_csv("forecast_accuracy_results.csv", index=False)

print(result)
